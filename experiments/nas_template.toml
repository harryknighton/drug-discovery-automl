type = 'nas'

seeds = [0]

[search]
algorithm = 'random'
max_evaluations = 500
search_space = 'simple'
noise_temperature = 0.1
noise_decay = 0.96
# proxy = JacobianCovariance

[data]
label_scaler = 'standard'
num_workers = 0

[training]
max_epochs = 100
early_stop_patience = 30