type = 'nas'

seeds = [0]

[search]
algorithm = 'random'
max_evaluations = 500
search_space = 'simple'
noise_decay = 0.96
# proxy = JacobianCovariance

[data]
label_scaler = 'standard'

[training]
max_epochs = 100
early_stop_patience = 30