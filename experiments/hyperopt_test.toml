type = 'nas'

seeds = [0]

[search]
max_evaluations = 1
search_space = 'simple'
proxy = 'NumParams'

[data]
label_scaler = 'standard'
num_workers = 0

[training]
max_epochs = 1
early_stop_patience = 30