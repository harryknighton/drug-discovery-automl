type = 'nas'

seeds = [0]

[search]
algorithm = 'random'
max_evaluations = 1
search_space = 'simple'
noise_temperature = 0.1
noise_decay = 0.1
proxy = 'NumParams'

[data]
label_scaler = 'standard'
num_workers = 0

[training]
max_epochs = 1
early_stop_patience = 30