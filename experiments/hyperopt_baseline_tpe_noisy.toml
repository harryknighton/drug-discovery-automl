type = 'nas'

seeds = [1515]

[search]
algorithm = 'tpe'
max_evaluations = 500
search_space = 'simple'
noise_decay = 0.96  # Decay to 1% over the first 100 epochs
loss_explainability_ratio = 1.0

[data]
label_scaler = 'standard'

[training]
max_epochs = 100
early_stop_patience = 30