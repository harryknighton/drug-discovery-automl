type = 'nas'

seeds = [2409]

[search]
algorithm = 'tpe'
max_evaluations = 500
search_space = 'simple'
noise_temperature = 0.1
noise_decay = 0.96

[data]
label_scaler = 'standard'
num_workers = 32

[training]
max_epochs = 100
early_stop_patience = 30